# 命令详细说明

## `/commander start [任务描述]`

启动新任务。

### 前置检查

在启动任务前，自动执行以下检查：

```
1. 检查 .info/usr.json 是否存在
   └─ 不存在 → 提示运行 /user-profile

2. 读取 usr.json.metadata.last_checked_at
   └─ 与 info/ 目录文件比较修改时间

3. 检查 info/ 目录是否有新文件
   └─ 有新文件 → 提示运行 /user-profile 更新
```

### 处理流程

| 步骤 | 操作 | 说明 |
|:-----:|------|------|
| 1 | 读取 tasks.json | 获取 next_id 和现有任务列表 |
| 2 | 分配任务 ID | 格式：k01, k02, k03... |
| 3 | 调用 skill-generator | 拆解任务，生成子技能列表 |
| 4 | 显示计划 | 展示任务步骤，等待用户确认 |
| 5 | 创建目录结构 | 创建 results/k01/ 目录 |
| 6 | **初始化 .reasoning.md** | **创建初始推理日志，记录任务启动（重要！）** |
| 7 | 生成子技能 | 为每个步骤创建独立的 skill |
| 8 | 更新 tasks.json | 记录新任务信息 |
| 9 | 创建 README.md | 在 results/k01/ 中创建任务总览 |

### 输出示例

```
═══════════════════════════════════════════════════════
新任务创建
═══════════════════════════════════════════════════════

任务 ID: k01
任务名称: 搭建 Next.js 博客
任务类型: web

执行步骤:
  1. k01_init_project    初始化项目
  2. k01_config_mdx      配置 MDX
  3. k01_create_layout   创建布局
  4. k01_article_list    文章列表
  5. k01_deploy          部署配置

是否确认创建此任务？[y/n]
```

## `/commander status`

显示全局状态概览。

### 输出格式

```
═══════════════════════════════════════════════════════
全局状态
═══════════════════════════════════════════════════════

用户画像: ✓ 已生成 (更新时间: 2026-01-27 14:30)

任务统计:
  总任务数: 3
  进行中:   2
  已完成:   1

任务列表:
  k01  搭建 Next.js 博客      [████░░] 3/5  进行中
  k02  文章搜索功能           [██░░░░] 1/4  进行中
  k03  用户认证系统           [█████] 5/5  已完成

═══════════════════════════════════════════════════════
```

### 数据来源

- 用户画像状态：检查 `.info/usr.json` 存在性及时间戳
- 任务统计：统计 `.info/tasks.json` 中的任务状态

## `/commander progress k01`

显示特定任务的详细进度。

### 输出内容

| 信息 | 来源 |
|-----|------|
| 任务基本信息 | tasks.json |
| 当前进度 | tasks.json.current_step |
| 执行记录 | results/k01/execution.md |
| 最新笔记 | results/k01/notes.md |

### 输出示例

```
═══════════════════════════════════════════════════════
k01 任务进度
═══════════════════════════════════════════════════════

任务名称: 搭建 Next.js 博客
当前状态: 进行中
进度: 3/5 步骤

已完成步骤:
  ✓ k01_init_project    初始化项目
  ✓ k01_config_mdx      配置 MDX
  ✓ k01_create_layout   创建布局

当前步骤:
  → k01_article_list    文章列表

待执行步骤:
  ⏭ k01_deploy          部署配置

最新笔记:
  - MDX 配置已完成，支持 frontmatter
  - 布局组件使用 Tailwind CSS

═══════════════════════════════════════════════════════
```

## `/commander list`

列出所有任务及其简要状态。

### 输出示例

```
ID    任务名称              状态      进度
k01   搭建 Next.js 博客     进行中    3/5
k02   文章搜索功能          进行中    1/4
k03   用户认证系统          已完成    5/5
```

## `/commander results k01`

显示任务结果文件。

### 操作

1. 列出 `results/k01/` 目录内容
2. 显示每个文件的简要说明
3. 询问用户是否查看某个文件

### 输出示例

```
results/k01/
├── README.md       # 任务总览
├── plan.md         # 任务计划
├── execution.md    # 执行记录 (3 条)
├── notes.md        # 笔记 (2 条)
└── artifacts/      # 生成的文件 (5 个)

查看哪个文件？[文件名 / skip]
```

## `/commander continue k01`

继续执行任务的下一步。

### 流程

1. 读取 tasks.json，获取 current_step
2. 显示当前步骤信息
3. 执行对应的子技能
4. 更新 current_step
5. 更新 execution.md

## `/commander complete k01`

手动标记任务为已完成。

### 操作

1. 更新 tasks.json.status = "completed"
2. 在 results/k01/README.md 添加完成时间
3. 提示是否归档

## `/commander archive k01`

归档已完成任务。

### 操作

1. 检查任务状态为 "completed"
2. 移动 results/k01/ → results/archived/k01/
3. 更新 tasks.json.status = "archived"

## `/commander upgrade`

查看并升级已完成任务的 k_ 技能为 p_ 技能（验证技能）。

### 自动检测

当任务通过 TaskUpdate 标记为 `completed` 时，`detect-task-complete.sh` Hook 会自动：

1. 扫描任务的 k_ 技能（如 `k01_research`, `k01_write_article`）
2. 设置 `tasks.json.tasks[k01].upgrade_pending = true`
3. 记录可升级技能列表到 `upgrade_candidates`

### 处理流程

| 步骤 | 操作 | 说明 |
|:-----:|------|------|
| 1 | 扫描 tasks.json | 查找 `upgrade_pending=true` 的任务 |
| 2 | 显示可升级技能 | 列出每个任务的 k_ 技能 |
| 3 | 用户选择 | AskUserQuestion 询问升级方式 |
| 4 | 执行升级 | 调用 `promote-to-proven.sh` |
| 5 | 清理标记 | 设置 `upgrade_pending=false` |

### 输出示例

```
═══════════════════════════════════════════════════════
技能升级
═══════════════════════════════════════════════════════

发现 2 个任务有待升级技能:

┌─────────────────────────────────────────────────────┐
│ 任务: k01 - 调研 openclaw 并写公众号推文            │
│ 状态: 已完成 (2026-01-31)                            │
│ 可升级技能:                                          │
│   • k01_research_openclaw                            │
│   • k01_write_article                                │
├─────────────────────────────────────────────────────┤
│ 任务: k02 - Claude Code 最近一个月更新              │
│ 状态: 已完成 (2026-02-01)                            │
│ 可升级技能:                                          │
│   • k02_research_claudecode                          │
│   • k02_write_article                                │
└─────────────────────────────────────────────────────┘

请选择升级方式:
- 全部升级 → 将所有任务的所有技能升级为 p_ 技能
- 选择升级 → 自定义升级哪些任务的哪些技能
- 跳过 → 保留 k_ 技能，稍后使用 /commander upgrade 处理
```

### 升级后效果

```
✅ 技能升级完成

k01_research_openclaw → p_research_open_source
k01_write_article → p_article_techar
k02_research_claudecode → p_research_claudecode
k02_write_article → p_article_techblog

已创建:
  .claude/skills/p_research_open_source/
  .claude/skills/p_article_techar/
  .claude/skills/p_research_claudecode/
  .claude/skills/p_article_techblog/

已更新:
  .info/tasks.json (proven_skills)

已归档:
  .claude/skills/.archived/k01_*/
  .claude/skills/.archived/k02_*/
```

### 技能命名建议

| 原始技能 | 建议命名 | 描述 |
|---------|---------|------|
| k01_research_openclaw | p_research_open_source | 调研开源项目的标准流程 |
| k01_write_article | p_article_techar | 撰写技术文章的执行方案 |
| k02_research_claudecode | p_research_claudecode | Claude Code 更新调研方法 |
| k02_write_article | p_article_techblog | 技术博客推文撰写模板 |

## `/commander cleanup`

清理低频使用的技能，基于阈值自动归档。

### 自动触发

当技能数量超过阈值时，`check-skills-limit.sh` Hook 会自动：

1. 检测技能数量（u_ > 5, p_ > 10, k_ > 20）
2. 设置 `tasks.json.archive_pending = true`
3. 记录超限原因到 `archive_reason`

### 阈值设置

| 技能类型 | 阈值 | 说明 |
|:---------|-----:|:-----|
| u_ 技能 | 5 | 用户技能，保持精简 |
| p_ 技能 | 10 | 验证技能，高质量集合 |
| k_ 技能 | 20 | 任务技能，允许较多 |

### 处理流程

| 步骤 | 操作 | 说明 |
|:-----:|------|------|
| 1 | 扫描 tasks.json | 检查 `archive_pending=true` |
| 2 | 统计技能数量 | 显示当前各类技能数 |
| 3 | 用户确认 | AskUserQuestion 询问是否清理 |
| 4 | 执行归档 | 调用 `archive-low-frequency-skills.sh` |
| 5 | 清理标记 | 设置 `archive_pending=false` |

### 输出示例

```
═══════════════════════════════════════════════════════
技能清理
═══════════════════════════════════════════════════════

当前技能统计:
  u_ 技能: 7 ⚠️ 超过阈值 (5)
  p_ 技能: 4 ✓ 正常
  k_ 技能: 8 ✓ 正常

超限原因: u_ 技能超过阈值 (7 > 5)

待归档技能 (按使用频率排序):
  1. u_old_react_classic (使用次数: 0)
  2. u_unused_python_tool (使用次数: 0)

请选择操作:
- 确认归档 → 归档上述技能
- 选择归档 → 自定义归档哪些技能
- 预览模式 → --dry-run 查看但不执行
- 跳过 → 保留所有技能
```

### 归档后效果

```
✅ 清理完成

已归档:
  u_old_react_classic → .claude/skills/.archived/u_old_react_classic
  u_unused_python_tool → .claude/skills/.archived/u_unused_python_tool

已更新:
  .info/tasks.json (user_skills)

归档日志: .claude/skills/.archived/.archive_log.json

💡 恢复技能: mv .claude/skills/.archived/<skill_name> .claude/skills/
```

### 智能归档规则

| 场景 | 优先级 | 说明 |
|:-----|-------:|:-----|
| 有 p_ 版本的 k_ 技能 | 最高 | 已升级的 k_ 技能优先归档 |
| usage_count = 0 | 高 | 从未使用的技能 |
| usage_count 最低 | 中 | 使用频率低的技能 |

### 命令行直接调用

除了通过 commander，也可以直接调用归档脚本：

```bash
# 自动归档（自动判断类型）
.claude/hooks/archive-low-frequency-skills.sh

# 归档特定类型
.claude/hooks/archive-low-frequency-skills.sh --type=user --count=1

# 预览模式
.claude/hooks/archive-low-frequency-skills.sh --dry-run
```
